addons:
- name: gng-nginx
  versions:
    current: ""
    desired: 4.13.0
  notes: ""
  source:
    chart: ingress-nginx
    repository: https://kubernetes.github.io/ingress-nginx
  warnings:
  - Pods crash immediately on ARM64 platforms with "illegal instruction" errors.
  - Nil pointer dereference crashes after node restarts causing pod instability.
  - NGINX controller pods fail to start if `proxy-buffer-size` is set without explicitly
    setting `proxy-busy-buffers-size`.
  compatible_k8s_versions:
    min: ""
    max: ""
  necessary_api_versions: []
  values_schema: ""
  opa_checks: []
  resources: []
