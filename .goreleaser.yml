## DO NOT EDIT - Managed by Terraform
# yaml-language-server: $$schema=https://goreleaser.com/static/schema.json

version: 2
project_name: gonogo
builds:
  - id: gonogo
    main: main.go
    binary: gonogo
    ldflags:
      - -X main.version={{.Version}} -X main.commit={{.Commit}} -s -w
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm
      - arm64
    goarm:
      - 6
      - 7
    ignore:
      - goos: windows
        goarch: arm
before:
  hooks:
    - go mod tidy
    - go generate ./...
brews:
  - name: gonogo
    repository:
      owner: FairwindsOps
      name: homebrew-tap
    directory: Formula
    description: Check cluster addons for upgrade confidence
    url_template: "https://github.com/FairwindsOps/gonogo/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    test: |
      system "#{bin}/gonogo version"
release:
  disable: '{{ eq (envOrDefault "GORELEASER_SKIP_RELEASE" "false") "true" }}'
  prerelease: auto
  github:
    owner: FairwindsOps
    name: gonogo
  footer: |
    You can verify the signatures of both the checksums.txt file and the published docker images using [cosign](https://github.com/sigstore/cosign).

    ```bash
    cosign verify-blob checksums.txt --signature=checksums.txt.sig  --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```

    ```bash
    cosign verify us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }} --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```
checksum:
  name_template: "checksums.txt"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
signs:
  - cmd: cosign
    args:
      - "sign-blob"
      - "--key=hashivault://cosign-p256"
      - "--output-signature=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: all
docker_signs:
  - artifacts: all
    args: ["sign", "--key=hashivault://cosign-p256", "us-docker.pkg.dev/fairwinds-ops/oss/gonogo@${digest}", "-r", "--yes"]
dockers:
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-amd64"
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-amd64"
     use: buildx
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/amd64"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-arm64v8"
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-arm64v8"
     use: buildx
     goarch: arm64
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm64/v8"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-armv7"
        - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-armv7"
     use: buildx
     goarch: arm
     goarm: 7
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm/v7"
docker_manifests:
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .Tag }}-armv7"
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/gonogo:{{ .FullCommit }}-armv7"
